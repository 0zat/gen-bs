let desc_output =
  "output file name"

let desc_dir =
  "input from files of a directory"

let desc_file =
  "input from a file"

let desc_external_type =
  "json file which has external defiend types in order to generate cast function" 

let desc = 
  "available options: -dir <dir>, -version, -external_type"

let desc_version =
  "show gen-bs version"

let desc_trace =
  "print backtrace when an error occurs"